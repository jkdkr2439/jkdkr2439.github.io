export const createCart=()=>[];export function addItem(cart,productId){const found=cart.find(x=>x.productId===productId);return found?cart.map(x=>x.productId===productId?{...x,quantity:x.quantity+1}:x):[...cart,{productId,quantity:1}]}export const setQuantity=(cart,id,q)=>Number.isInteger(q)&&q>0?cart.map(x=>x.productId===id?{...x,quantity:q}:x):cart;export const removeItem=(cart,id)=>cart.filter(x=>x.productId!==id);export const cartTotal=(cart,products)=>cart.reduce((sum,line)=>sum+(products.find(x=>x.id===line.productId)?.priceCents||0)*line.quantity,0);

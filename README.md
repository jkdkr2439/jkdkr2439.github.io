# Danh Nghĩa Hệ — next

Modular successor to `jkdkr2439.github.io`, built alongside the current production site.

Before committing content or navigation changes:

```powershell
python scripts/validate_site.py
```

See `ARCHITECTURE.md` for ownership boundaries and invariants.
